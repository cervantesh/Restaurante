@model IEnumerable<ProyectoRestaurante.Models.Sucursal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.longitud)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.latitud)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FKRestauranteID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FKDireccionID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.longitud)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.latitud)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FKRestauranteID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FKDireccionID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SucursalID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SucursalID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SucursalID })
        </td>
    </tr>
}

</table>
